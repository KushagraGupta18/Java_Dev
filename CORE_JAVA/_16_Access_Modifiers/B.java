public class B {
    
    int marks=20;//can be accessed from same package because when we don't write any access modifier then by default it takes default access modifier and that can be accessed from same package
    private int age=30;//can be accessed from the same class only

}
